/** Platform-facing speech-to-text port; the legacy capability owns its shape. */
export type {
  SpeechToText,
  Transcript,
  TranscriptSegment,
  TranscriptWord,
} from "../../../capabilities/speech-to-text";
